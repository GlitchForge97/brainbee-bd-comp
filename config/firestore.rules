rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to CREATE and READ registrations
    match /registrations/{document=**} {
      allow create, read;
      allow update, delete: if false;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}

